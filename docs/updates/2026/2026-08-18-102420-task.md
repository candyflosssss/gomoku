# 任务更新：合并强化 AI 并增加先手禁手开关

- 时间：2026-08-18T10:24:20+08:00
- 类型：task
- 状态：completed

## 摘要

合并贡献者的强化 AI PR，并在公开仓库与本地工作区增加默认开启的人机先手禁手开关，统一约束玩家和三档 AI 的黑棋落子。

## 具体更新

- 批准并合并 PR #2，纳入强化 hard AI 与 Renju 规则 API。
- 创建并合并 PR #3，在人机开始设置增加先手禁手开关；开启时拒绝玩家黑棋三三、四四和长连，并保证三档 AI 返回合法黑棋落点。
- 保留开关状态用于重新开始和再来一局，并在对局顶部显示当前启用先手禁手。
- 浏览器测试支持 GOMOKU_CHROME 路径，并在浏览器启动失败时清理本次测试进程。
- 将已合并的 AI 核心、界面和测试同步到当前包含 Logto 与联机修复的本地工作区，未推送这些既有未提交改动。

## 涉及文件

- `package.json`
- `web/gomoku-core.js`
- `web/index.html`
- `tests/core-ai.test.mjs`
- `tests/browser.test.mjs`

## 验证

- 干净分支 npm run check：通过（核心与联机集成）。
- 最终核心 npm run check:core：通过，覆盖 easy、medium、hard 在自由规则和先手禁手模式下的合法落子。
- 当前实际工作区 npm run check：通过（AI 核心、Logto 认证、房间/P2P/复用回归）。
- 内联页面脚本语法与先手禁手 UI 契约检查：通过；git diff --check 与敏感信息扫描：通过。
- npm run check:browser：未进入测试用例；系统 Chrome 与 Playwright Chromium 147 均在启动阶段 SIGTRAP 崩溃，测试进程和临时目录已清理。
- GitHub 状态：PR #2 与 PR #3 均已合并到 main。
